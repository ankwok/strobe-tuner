<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StrobeTuner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/dark_strobe</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.Settings" parent="Theme.Material3.DayNight">
        <item name="titleTextColor">@color/brown</item>
        <item name="android:statusBarColor">@color/dark_strobe</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/brown</item>
        <item name="colorPrimaryContainer">@color/light_3500k</item>
        <item name="colorSecondary">@color/brown</item>
    </style>
</resources>